# 基础科学计算库
numpy>=1.21.0,<1.24.0  # 可以允许向后兼容的更新
pandas>=1.3.0,<1.6.0  # 保持一定的版本范围

# 可视化库
matplotlib>=3.4.0,<3.7.0  # 限制在合理范围内，避免不兼容的更新
seaborn>=0.11.0,<0.12.0  # 以保持与matplotlib的兼容性

# 机器学习库
scikit-learn>=0.24.0,<1.2.0  # 保持兼容性并支持近期更新
xgboost>=1.5.0,<2.0.0  # 同理，避免大版本跳跃导致不兼容
tensorflow>=2.8.0,<2.12.0  # 版本范围设置合理，避免不必要的版本跳跃

# 其他工具库
scipy>=1.7.0,<1.10.0  # 与numpy兼容的版本范围
joblib>=1.0.1,<1.3.0  # 保持在稳定范围内
